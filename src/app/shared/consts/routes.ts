import { DynamicKeyModels } from "../../core/models/dynamic.key.models";

const PATHNAMES: DynamicKeyModels = {
	main: '/',
  film:'film',
  favorites: 'favorites',
  sign_up: 'sign-up',
  sign_in: 'sign_in',
  error: 'error',
  reject:'*',
};

export { PATHNAMES };
